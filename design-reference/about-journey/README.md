# About page — product journey graphic (reference)

Rob-approved preview of the "product journey" section on the About page
(the `.about-journey` block in `frontend/index.html`), captured 2026-08-19.

- `desktop-1.png`, `desktop-2.png`, `desktop-3.png` — the desktop horizontal
  scroll-rail, captured in three segments left to right (steps 1–9).
- `mobile.png` — the mobile vertical timeline version (same 9 steps, real
  components, not the desktop layout shrunk down).

Kept here as a fallback reference in case this section needs to go through
an external design tool later — these screenshots show the approved
content/sequence even if the visual treatment changes.
